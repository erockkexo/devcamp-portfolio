<h1>Create a new post</h1>


<%= form_for(@portfolio_item) do |form| %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :subtitle %>
    <%= form.text_field :subtitle %>
  </div>




  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>


<%-# here we are going to create another form for the new technologies attribute
 #pulls from the controller page -%>


  <ul>
    <%-# this is a special command to create nested forms and attributes
    #pulls from controller page 
    #this of course only affects the new page, we will have to modify the show page -%>

    <%= form.fields_for :technologies do |technology_form| %>
    <li>
      <%= technology_form.label :name %>
      <%= technology_form.text_field :name %>
    </li>
    <% end -%>
  </ul>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>